const Multiplicacao = (numero1: number, numero2: number): number => numero1 * numero2;

console.log(Multiplicacao(5, 10));

const DizOla = (nome: string) => console.log(`Olá ${nome}`);

console.log(DizOla('Pedro'))
